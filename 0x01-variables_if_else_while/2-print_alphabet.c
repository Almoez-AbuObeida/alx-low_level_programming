#include <stdio.h>

/**
*main - main function
(*
*Description: this function will print the alphabet.)
*Return: 0
*/
int main(void)
{
	char n = 'a';

	while (n <= z)
	{
		putchar('%c', n);
		n++;
	}
	putchar('\n');

	return (0);
}
