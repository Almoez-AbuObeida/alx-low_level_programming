#include "main.h"

/**
* puts2 - a function that prints every other character of a string.
* @str: string
* Return: void
*/

void puts2(char *str)
{
	int i = 0;

	while (s[i] != '\0')
	{
		_putchar(s[i]);
		i += 2;
	}
}
